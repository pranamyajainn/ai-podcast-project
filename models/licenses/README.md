# License Notes

Current active validation model:

- MuseTalk: see `models/MuseTalk/LICENSE` after submodule initialization.

No model weights are committed. Store any downloaded weight license notes here if future work requires an auditable record.
